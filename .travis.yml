sudo: false
language: c
compiler: gcc
install:
- export CC="gcc-5"
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5
    - clang
    - libcunit1
    - libcunit1-dev
    - libcurl4-openssl-dev
    - libjansson-dev
script: make CC=gcc-5 test
